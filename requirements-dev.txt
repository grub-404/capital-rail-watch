# Dev / CI — install alongside backend/requirements.txt
pytest>=8.0
# Vision slice 1+ (YOLO inference & tests; pulls PyTorch)
ultralytics>=8.0
# Vision slice 2+ (YouTube clip downloads)
yt-dlp>=2024.1.0
# Label assist maps (median bg, edges, SLIC superpixel borders in ximgproc)
opencv-contrib-python-headless>=4.8.0
